# PostgreSQL (local: docker compose up postgres -d)
DATABASE_URL=postgresql://movasseghi:movasseghi@localhost:5432/movasseghi

# Generate: openssl rand -hex 32
PAYLOAD_SECRET=change-me-in-production

# Public site URL (staging/production)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Payment gateways (optional until checkout goes live)
# NEXTPAY_API_KEY=
# ZARINPAL_MERCHANT_ID=
