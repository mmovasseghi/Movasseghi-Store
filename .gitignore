# Legacy forensic extraction workspace (derived from read-only backup)
.legacy-extract/

# Local raw backup — too large, contains secrets
Old Verison WebSite/

# Temp / clone check
_remote-check/

# Environment and secrets
.env
.env.*
!.env.example
*.pem
*.key
id_rsa*
secrets/

# Dependencies and build
node_modules/
.next/
dist/
build/
coverage/
.turbo/

# OS / editor
.DS_Store
Thumbs.db
*.log

# Never commit legacy credentials
**/wp-config.php
**/*.sql
**/*.sql.gz
**/*.tar.gz

# Separate Legacy repo working copy (published to Movasseghi-Store-Legacy)
legacy-export/
